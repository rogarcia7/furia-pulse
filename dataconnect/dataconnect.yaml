specVersion: "v1"
serviceId: "furia-pulse"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "furia-pulse-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
